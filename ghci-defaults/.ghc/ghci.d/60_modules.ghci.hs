-- useful imports
import Prelude hiding(sequence, sequence_, mapM, mapM_, all, any, sum, product, minimum, maximum, elem, notElem, concat, concatMap, id, or, and, foldr, foldl, return, (.))
import Control.Category
import Control.Arrow
import Data.Functor
import Control.Applicative
import Control.Monad hiding(sequence, sequence_, mapM, mapM_, forM, forM_, msum)
import Data.Monoid
import Data.Foldable
import Data.Traversable
