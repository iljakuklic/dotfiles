# useful git aliases
[alias]
	s = status
	graph = log --graph --oneline --decorate

# turn the colours on
[color]
	ui = auto
	branch = auto
	diff = auto
	grep = auto

# push behaviour
[push]
	default = simple

[include]
	path = ~/.config/git/colours.conf
